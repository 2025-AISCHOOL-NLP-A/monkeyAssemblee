<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>전체 회원 목록입니다.</h1>
    <ul>
        <!-- for문을 통해서 사용자 목록 출력 -->
        <% for(let i=0; i<data.length; i++){ %>
            <li>이름 : <%= data[i].name %>, 나이: <%= data[i].age %></li>
        <%} %>

        <!-- for each문 사용 -->
        <% data.forEach((item)=>{ %>
            <li> 이름 : <%=item.name %> , 나이 : <%=item.age%></li>
        <%})%>
    </ul>

</body>
</html>