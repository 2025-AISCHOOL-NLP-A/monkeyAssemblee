<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        h1{
            color: brown;
            
        }
        th{
            color: darkorange;
        }
        td {
            text-align: center;
            color: darkgreen;
        }
    </style>
</head>
<body>
    <h1>우리반 게시판입니다.</h1>
    <table border="1">
        <tr>
            <th>번호</th>
            <th>제목</th>
            <th>내용</th>
            <th>작성자</th>
            <th>조회수</th>
        </tr>
        <!--
            미션2 : 넘겨받은 posts키에서 값을 추출
            * 문법 : 반복문(for)
        -->
        <%for(let i =0; i<posts.length; i++){%>
        <tr>
            <td><%= posts[i].id %></td>
            <td><a href="/board/<%=posts[i].id%>"><%= posts[i].title %></a></td>
            <td><%= posts[i].content %></td>
            <td><%= posts[i].writer %></td>
            <td><%= posts[i].hit %></td>
        </tr>
        <%}%>

                <!-- <%posts.forEach((item)=>{ %>
        <tr>
            <td><%= item.id%></td>
            <td><a href="/board"><%= item.title%></a></td>
            <td><%= item.content%></td>
            <td><%= item.writer%></td>
            <td><%= item.hit%></td>
        </tr>    
        <% }); %> -->
    </table>
    <a href="/write">게시글 작성</a>

</body>
</html>